# =============================================================================
# SCHOLAR AI PROJECT - GITIGNORE
# =============================================================================
# This file consolidates ignore patterns for both backend and frontend
# Best practice: Single .gitignore at project root for monorepos
#
# Structure:
# - Backend patterns: Go-specific files, build outputs, test artifacts
# - Frontend patterns: Node.js dependencies, build outputs, cache files  
# - Shared patterns: IDE files, logs, temporary files, environment variables
# - Project specific: Configuration files with secrets, documentation
#
# Note: Individual .gitignore files in  and frontend/ have been removed
# to avoid conflicts and maintain consistency across the monorepo.

# =============================================================================
# BACKEND (Go/API) - Ignore Patterns
# =============================================================================

# Go binaries and executables
*.exe
*.exe~
*.dll
*.so
*.dylib
web
main
server

# Go test binaries
*.test

# Go coverage tool output
*.out
coverage.txt
coverage.html
coverage/
*.cover

# Go workspace files
go.work
go.work.sum

# Go dependencies
vendor/

# Go build output
bin/
dist/
build/

# Go profiling data
*.prof
*.pprof
cpu.pprof
mem.pprof

# Go race detector output
*.race

# Go module cache (uncomment if needed)
# go.sum



# =============================================================================
# SHARED - Common Ignore Patterns
# =============================================================================

# Environment variables - CRITICAL: never commit secrets
.env
.env.local
.env.development
.env.test
.env.production
.env.*.local

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Logs
*.log
logs/
*.log.*

# Temporary files
tmp/
temp/
*.tmp
*.bak
*.backup

# Database files (local development)
*.db
*.sqlite
*.sqlite3

# Configuration files with secrets
config/development.yaml
config/production.yaml
config/local.yaml

# RSA keys and certificates (CRITICAL: never commit private keys)
keys/
*.pem
*.key
*.crt
*.cert
private_key.pem
certificate.pem

# =============================================================================
# PROJECT SPECIFIC - Scholar AI
# =============================================================================

# API documentation (auto-generated)
docs/docs.go
docs/swagger.json
docs/swagger.yaml

# Air live reload (Go hot reload)
.air.toml

# Build artifacts
web

server
